#!/bin/bash
#
# Put here all commands to run anytime the plugin installed (e.g. at boot-time), re-installed or updated. 
# They are executed as NON-BLOCKING, to ensure that boot/install sequences proceed in case of any errors.
# The purpose, as an example, is to establish that certain packages are always installed by means of `pip`.
# 
